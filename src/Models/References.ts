export class References {
    number: number = 0; //Folio del documento de referencia (String).
    referenceDate: number = 0;//Fecha del documento de referencia (Integer).
    reason: string | null = null;// Razón del documento (String).
    codeSii: number = 0;//Código tributario del documento de referencia (Integer).
}