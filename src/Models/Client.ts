export class Client {
    code: string | null = null; //Rut del cliente
    city: string | null = null; //Ciudad del cliente 
    company: string | null = null; //Compania del cliente
    municipality: string | null = null; //Comuna del cliente
    activity: string | null = null; // Giro del cliente
    address: string | null = null; // Direccion del cliente
    isForeigner: number = 0;//Si es extranjero es 1
}